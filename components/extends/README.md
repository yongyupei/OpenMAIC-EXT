# components/extends

Fork UI. Mirror upstream areas: `stage/`, `scene-renderers/`, `settings/`, `generation/`, etc.

Use `Ext` / `Custom` prefixes for new components. See `extends/DEVELOPMENT_GUIDE.md`.
