import { test } from '@playwright/test';

test.describe.skip('extends e2e', () => {
  test('placeholder — add specs under e2e/extends/', async () => {
    // Fork-specific Playwright tests go here.
  });
});
