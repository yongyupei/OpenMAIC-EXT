/** Tailwind classes: fixed column height (viewport-based max) with internal scrolling. */
export const knowledgeBasePanelShellClassName =
  'flex h-[calc(100dvh-8rem)] max-h-[calc(100dvh-8rem)] min-h-[560px] flex-col overflow-hidden';
